/**
 * Configuration file for Supabase credentials
 * Replace these with your actual Supabase project credentials
 */

// TODO: Replace with your actual Supabase credentials from https://supabase.com/dashboard/project/_/settings/api
window.VITE_SUPABASE_URL = 'YOUR_SUPABASE_URL_HERE';
window.VITE_SUPABASE_PUBLISHABLE_KEY = 'YOUR_SUPABASE_ANON_KEY_HERE';

// You can also use environment variables if deploying to a platform like Netlify or Vercel
// In that case, the build process would inject these values
